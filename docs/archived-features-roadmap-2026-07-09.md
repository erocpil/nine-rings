# Nine Rings — 功能统计与路线图

> 原始生成日期：2026-07-09
> 状态复核：2026-07-30
> 本文仅保留历史基线；当前计划以 [`docs/ROADMAP.md`](./ROADMAP.md) 和
> [`docs/engineering-improvement-plan.md`](./engineering-improvement-plan.md) 为准。

---

## 已完成功能（40+ 项）

### 笔记系统
- 按天组织笔记
- 富文本编辑（TipTap / Quill Delta）
- 笔记 CRUD（创建/读取/更新/删除 + 软删除）
- 笔记置顶
- 笔记拖拽排序
- 跨日移动
- 版本历史（自动快照，最多保留 30 版，可回滚）
- 回收站（恢复 + 永久删除 + 自动清理过期）
- 键盘快捷键（Ctrl+N 新建, Ctrl+E 搜索, Ctrl+, 设置）

### 待办系统
- 添加/勾选/删除（带撤销）
- 拖拽排序（▲▼ 按钮）
- 双击编辑
- 过期待办提示（自动扫描未完成的历史待办）
- 添加待办后自动保存

### 标签系统
- 笔记标签输入、存储
- 侧栏标签筛选（点击标签筛选当前日期笔记）
- 标签支持多标签

### UI 布局
- 三栏布局（侧栏笔记列表 + 待办区 + 笔记编辑器）
- 左右分隔条可拖拽（120–500px）
- 上下分隔条可拖拽（待办/编辑器比例）
- 响应式 flex 布局
- 多窗口支持（Tauri 桌面端）

### 主题
- 系统主题、浅色、深色
- 粋（护眼绿）、雅（淡紫）、幟（鹅黄）

### 搜索
- 全文搜索（搜索笔记标题 + 正文）
- 搜索结果展示

### Header
- 时钟显示（24h 格式，每 30 秒更新）
- 日期选择器（📅 emoji 触发系统日历）
- 每日概览（周几 月/日 · 笔记数 · 待办进度）
- 搜索框（右侧紧凑布局）

### 配置系统
- 设置面板（主题、字号、默认视图、待办继承开关、自动清理天数）
- 持久化到 localStorage / SQLite

### 数据
- IndexedDB 存储（Web 端）
- SQLite 存储（Tauri 桌面端，Rust 后端）
- 抽象 StorageAdapter 层，前后端共享数据模型
- JSON Schema 定义（`schema/` 目录）
- Quill Delta 格式，带 Delta ↔ Markdown 转换层

### 导出/导入
- API 层：全量导出 JSON、导入 JSON、单篇 Markdown 导出
- UI：无（仅 API，待添加）

---

## 历史待优化项（已迁移）

| # | 优化项 | 说明 | 优先级 |
|---|--------|------|--------|
| 项目 | 当前状态 |
|------|----------|
| 日历月视图 | 仍待做，转由 `ROADMAP.md` 跟踪 |
| 待办跨日继承 UI 开关 | 已实现，设置面板已有开关 |
| 搜索结果高亮 | 已实现，搜索结果支持片段与高亮 |
| 笔记间快速切换 | 仍待做，转由 `ROADMAP.md` 跟踪 |
| 长文本折行 | 已有响应式布局，暂无独立 TODO |
| 空状态优化 | 部分完成，暂无独立 TODO |
| 批量操作 | 已实现部分批量删除/只读，完整批量编辑仍待做 |
| 设置面板 tab 分页 | 尚未列入当前主路线图 |

---

## 历史待添加功能状态（2026-07-30 复核）

| # | 功能 | 当前状态 | 说明 | 优先级 |
|---|------|----------|------|--------|
| 功能 | 当前状态 | 备注 |
|------|----------|------|
| 标签管理 | 已实现 | 设置面板支持重命名、合并、删除 |
| 云端同步 | 已改名为 GitHub 备份 | 当前是全量快照备份，不是实时同步 |
| 导出/导入 UI | 已实现 | 设置面板提供入口 |
| PWA 离线缓存 | 已实现 | Service Worker 已接入 |
| 更多键盘快捷键 | 部分实现 | 仍有快捷键体验待完善 |
| 每日回顾提醒 | 待做 | 当前路线图未单独承诺版本 |
| 笔记模板 | 基础版已实现 | 自定义模板仍待做 |
| Markdown 导入/导出 | 部分实现 | 当前已有导入及单篇导出，批量导出仍待做 |
| 待办定时提醒 | 已实现 | 支持 datetime-local 和浏览器通知 |
| 系统托盘 + 全局快捷键 | 已实现 | Tauri 桌面端已接入 |
| 图片附件 | 已实现 | 编辑器支持粘贴/拖拽图片 |
| 笔记锁定/加密 | 待做 | 仍在产品路线图中 |
| Flutter 移动端 | 部分实现 | APK 可构建，功能仍需与 Web/Tauri 对齐 |

---

## 历史说明

本文中的“未实现/存桩”描述来自 2026-07-09，部分已被后续实现或重新命名。
不要直接从本文创建新任务；新增任务应写入当前路线图或工程改进计划，并注明复核日期。

## 云端同步思路（历史 #2）

详见 `docs/sync-architecture.md`
